consoleLog("Filter + Map in JavaScript ");

var numbers = [8,1,7,4,6,5,9];

function oddNumber(element) {
    return element % 2;
}

var filtered = numbers.filter(oddNumber);

consoleLog( mappped );

var mappped = filtered.map(Math.sqrt);

consoleLog( mappped );
